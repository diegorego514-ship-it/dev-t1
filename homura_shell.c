#include <stdio.h> 
#include <stdlib.h>

int main(){

int shell;
printf("input the kind of shell program do you want to create: ");
scanf("%d", &shell);
if (shell);
// printf("input the name of the program that you want to create:") 
}